export interface Racquet {
  id: number;
  brand: string;
  model: string;
  series: string;
  weight: string;
  gripSize: string;
  balance: string;
  flex: string;
  stringTension: string;
  price: number;
  image?: string;
  description: string;
  stock: number;
}
